repositories {
    mavenLocal()
    maven {
        url = uri 'https://repo.maven.apache.org/maven2/'
    }
}

dependencies {
    compileOnly project(':library-basic')
    compileOnly project(':library-chat')
}

group = project.group
version = '2.0-SNAPSHOT'
archivesBaseName = 'channel-api'